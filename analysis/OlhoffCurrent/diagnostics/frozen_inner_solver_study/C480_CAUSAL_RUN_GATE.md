# Future C480 causal-run gate

ONE_C480_CORRECTED_INNER_SOLVER_RUN_JUSTIFIED

Exactly one future C480 treatment is scientifically justified because the existing N=2 configuration has an exact conic formulation and the frozen solver discrepancy is material. The future experiment must first implement and validate an audit-backed adapter that assembles every original constraint and requires a fresh certificate on each subproblem. That integration work and the topology run are outside this task. The gate establishes eligibility to attempt the one controlled experiment, not a guarantee that every future state will pass or that SOCP will improve the final topology. Any unsupported state or certificate failure must stop the treatment under the declared fallback. No additional optimization run, filter change or performance campaign is authorized by this result.

The future control is the validated three-rung C480 canary. Treatment keeps mesh,
initialization, filter, move, controller, p/q, projection and multiplicity settings
identical; only the inner problem-(25) solver changes. Compare omega1, Mnd, gray,
mid and broad-gray-core fractions, complete trajectory/controller events,
cumulative inner work, physical and filtered KKT, and topology. Exactly one
future treatment is contemplated, not a campaign. No topology run occurs here.
