# Validation outputs only

Artifacts here are implementation tests and read-only replays, not iteration-efficiency
scientific results. `offline_topology_replay.json` rechecks frozen Olhoff evidence;
`observer_*.mat` and `observer_unit.mat` are tiny instrumentation-smoke artifacts.

