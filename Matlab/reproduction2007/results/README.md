# Run-time output

Written by `run_repro2007(struct(..., 'save_results', true))` and by the
original reproduction scripts under `runs/`.

Contents are **git-ignored on purpose**. Frozen artifacts that the migration
regression compares against live in `../baseline/`, not here — do not write
run output into that directory.
