# Cross-method qualification plan

Status: **required; not executed**.

Use identity-preserving short trajectories from Proposed, Yuksel, Olhoff-LP, and any
selected Olhoff-MMA route. Independently recompute the Candidate C modal sequence and
compare evaluator output, diagnostics, selected ordinal, Q, topology, and indexing with
the harness. Exercise early, transition, late, and classifier-disagreement states.

The pass artifact must use schema `candidate_c_cross_method_qualification_v1` and bind
the classifier, evaluator and contract hashes, method/route set, optimizer source hashes,
state fingerprints, comparison tolerances, all outcomes, and `pass=true`.

