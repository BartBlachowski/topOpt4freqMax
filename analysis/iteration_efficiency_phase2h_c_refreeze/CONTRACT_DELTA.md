# Phase 2H contract delta

The contract advances from schema 2A.1 to 2H.1. It freezes Candidate C actual-gray mode
selection, Olhoff Eq. 4a mass for E2/E3, the unanimous three-diagnostic classifier,
adaptive mode search, `STRUCTURAL_MODE_NOT_FOUND`, binary-D exclusion, and separate
Olhoff LP/MMA routes and accounts.

Unchanged scientific constants and algorithms include the nine meshes, domain, volume
fraction, hard topology gate, `P=100`, `B_ref=3200`, `L_ref=500`,
`epsilon_ref=0.001`, `B_meas=min(max(B0,b_ref+P-1),B_ref)`, q levels
0.98/0.99/0.995, persistence, timing firewall, scaling, and censoring.

Three hash-bound qualifications are now mandatory: precision, cross-method, and
reference-length. The contract remains self-unauthorized and records production status
`BLOCKED`.

