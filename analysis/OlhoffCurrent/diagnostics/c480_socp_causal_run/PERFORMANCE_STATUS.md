# PERFORMANCE_STATUS — Part 18

```
PERFORMANCE_CAMPAIGN_STILL_BLOCKED
```

Preregistered rule: RECONSIDERATION_JUSTIFIED only if causal = A, realization =
HEALTHY and coverage = PASS. None of the three holds (E / INCONCLUSIVE / FAIL).
`PERFORMANCE_CAMPAIGN_CAN_RESUME` is never issued by this study.

The blocking reasons inherited from `three_rung_canary_preflight`
(`THREE_RUNG_CONTROLLER_GENERALIZES_BUT_FINE_MESH_SCIENCE_SUSPICIOUS`) and
`frozen_inner_solver_study` stand unchanged. The fine-mesh grayness at C480 still
has no established cause.

Cost note, not a performance claim: a certified `schur` SOCP inner solve costs about
37 s per outer iteration at 480×60 in early stage 1, about 23× the MMA inner loop at
the same stage (`COST_ANALYSIS.md`). Any future SOCP-based campaign would also need a
performance assessment. That assessment is premature until the apex certificate exists.

No nine-mesh campaign, and no run at any other mesh, was executed.
