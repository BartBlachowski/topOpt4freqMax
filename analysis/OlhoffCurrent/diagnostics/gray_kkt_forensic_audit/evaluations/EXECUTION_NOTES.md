OFFLINE FROZEN-STATE ANALYSIS — NOT OPTIMIZATION

Initial sandbox MATLAB startup exited without output. Authorized evaluator then ran outside sandbox. First attempt completed 400 and stopped before 480 FD because geometry sample generation had not finished. Resumed after all samples existed. Inspection found MATLAB concatenation coerced FD rows to integer because element IDs were integer; console calculations remained floating point but saved FD tables were unusable. Invalid files are explicitly named INVALID_INTEGER_SERIALIZATION, retained as scratch, never used in conclusions. The audit-only recorder now converts IDs to double; the same frozen states and identical preregistered samples/deltas were reevaluated. No production edits, optimizer calls or density updates occurred. Geometry read was interrupted and changed from repeated compressed-row reads to one full read per existing trajectory. A dimension orientation and JSON scalar serialization issue were corrected in audit scripts.
