{
  "schema_version": "conference_benchmark_timing/1",
  "common": {
    "total_wall_time_s": "Caller-side tic/toc around the solve and nothing else. CSV/JSON/MAT writing, plotting, table formatting, LaTeX generation, the common E1/E2/E3 evaluator and topology rendering are all outside it.",
    "overhead_time_s": "Whatever the solve spends outside the two named stages: configuration dispatch, model build, filter preparation, the final modal analysis and result assembly. It exists so that no microsecond has to be forced into a named stage.",
    "identity": "total_wall_time_s = time1 + time2 + overhead_time_s",
    "timing_accounting_residual_s": "total_wall_time_s minus (time1 + time2 + overhead_time_s).",
    "timing_accounting_fail": "TIMING_ACCOUNTING_FAIL: set when the residual exceeds the predeclared tolerance recorded alongside it.",
    "independent_crosscheck_residual_s": "A SECOND, independent measurement of the same interval: the caller-side total minus the solver's own self-reported wall time. Unlike the identity above it is not an algebraic tautology, so it detects a mis-nested timer.",
    "threads": "All runs are single-threaded (maxNumCompThreads(1)).",
    "memory": "NOT MEASURED and NOT REPORTED. Reliable, method-independent peak-memory measurement was not available in the MATLAB environment; memory was omitted rather than reported with inconsistent semantics."
  },
  "methods": {
    "Proposed": {
      "architecture": "one reference eigenanalysis, then a SIMP topology optimization",
      "count1": {
        "name": "eigenanalysis_solves",
        "value": "always 1",
        "meaning": "The number of reference eigenanalyses performed. This is a SOLVE count. It is NOT an optimization iteration and must not be read as one."
      },
      "count2": {
        "name": "simp_iterations",
        "meaning": "SIMP topology-optimization iterations."
      },
      "time1": {
        "name": "stage1_eigenanalysis_and_preparation_s",
        "boundary": "solver initialization: mesh and element matrices, boundary conditions, filter preparation, and the single reference eigenanalysis. stage1_reference_eigen_time_s isolates the eigenanalysis alone and is a SUB-INTERVAL of this."
      },
      "time2": {
        "name": "stage2_simp_time_s",
        "boundary": "the SIMP optimization loop only."
      }
    },
    "Yuksel": {
      "architecture": "two sequential optimization stages",
      "count1": {
        "name": "stage1_iterations",
        "meaning": "Stage-1 optimization iterations."
      },
      "count2": {
        "name": "stage2_iterations",
        "meaning": "Stage-2 optimization iterations."
      },
      "time1": {
        "name": "stage1_time_s",
        "boundary": "the Stage-1 loop only."
      },
      "time2": {
        "name": "stage2_time_s",
        "boundary": "the Stage-2 loop only; disjoint from Stage 1."
      },
      "assertions": [
        "N_total = N1 + N2",
        "T1 + T2 = optimization_loop_time (non-overlapping timers)"
      ]
    },
    "DuOlhoffReconstruction": {
      "label": "Du-Olhoff reconstruction (Pedersen stiffness + linear mass, adaptive box)",
      "preset": "duOlhoffPedersenAdaptiveBoxSensitivityFiltered",
      "upstream_preset": "duOlhoffAdaptivePedersen",
      "upstream_commit": "253069262407885a8b759a9e721c4f0a7d3a397d",
      "architecture": "NESTED: an outer loop, each of whose iterations contains a complete MMA sub-optimization.",
      "count1": {
        "name": "outer_iterations",
        "meaning": "Fig. 1 outer iterations."
      },
      "count2": {
        "name": "inner_mma_iterations_total",
        "meaning": "cumulative nested MMA sub-iterations over the whole run."
      },
      "time1": {
        "name": "outer_time_excluding_inner_s",
        "boundary": "every computational region of an outer iteration EXCEPT the nested MMA solve: FE assembly, the eigenproblem, modal processing, generalized-gradient sensitivities, sensitivity filtering, local subproblem construction, the step controller, the design update, bookkeeping and the convergence test. Measured as sum(t_outer) - sum(t_inner) from per-iteration timers, so the nested solve cannot be double-counted."
      },
      "time2": {
        "name": "inner_mma_time_total_s",
        "boundary": "the nested MMA loop, and only that."
      },
      "derived": [
        "inner_iterations_per_outer_mean",
        "inner_time_per_outer_mean_s",
        "inner_time_per_inner_iteration_mean_s",
        "inner_time_share_pct"
      ],
      "per_outer_iteration_cost": {
        "why": "the outer iteration count need not be monotone in the mesh, so total wall time alone cannot be read as a per-iteration scaling law",
        "fields": [
          "outer_time_total_s = sum(t_outer)",
          "outer_time_per_outer_mean_s = sum(t_outer)/N_outer",
          "outer_time_per_outer_median_s = median(t_outer)",
          "outer_time_excluding_inner_per_outer_mean_s = (sum(t_outer)-sum(t_inner))/N_outer",
          "eigen_time_per_outer_mean_s = sum(t_eig)/N_outer (FE assembly + eigensolve)",
          "gradient_time_per_outer_mean_s = sum(t_grad)/N_outer",
          "total_wall_time_per_outer_s = total_wall_time_s/N_outer",
          "inner_time_per_outer_mean_s, inner_time_per_inner_iteration_mean_s"
        ]
      },
      "prohibited": "N_outer + N_inner must NEVER be reported as a generic iteration count: the two are different objects.",
      "caveat": "Du-Olhoff RECONSTRUCTION, Pedersen/adaptive-box formulation (class C controller and filter radius, class B/D material law): SIMP p = 3 with the Pedersen (2000) linearized low-density stiffness (rho*rho0^(p-1) below rho0 = 0.1) and LINEAR mass, eq. (2); a per-element ADAPTIVE move box (initial 0.10, floor 0.002, x1.2 on monotone / x0.7 on reversing outer steps); NATURAL termination by ||drho||_2 < 0.05*sqrt(NE/3200) with no guards and no persistence, a heuristic design-change stop and not a KKT certificate; Sigmund sensitivity filter on all f_sk at a FIXED PHYSICAL radius R = 0.06 (1.2 elements at 160x20, 6 at 800x100). This is a DISTINCT formulation from the historical SIMP + eq. (4b) OlhoffCurrent reconstruction (duOlhoffSimpEq4bBetaStallLadderSensitivityFiltered), not a bug fix of it; Pedersen stiffness is the alternative Du & Olhoff sec. 2.2 name, not their choice. Terminal bimodality is NOT expected beyond coarse meshes: in the committed R = 0.06 sweep the optimum is bimodal only at 160x20 (native gap12 0.7 %) and native gap12 is 11.8-24.5 % from 240x30 to 800x100. Native eigenfrequencies are those of the Pedersen/linear-mass model; any cross-method table must name its evaluator model. Must NOT be labelled \"Olhoff 2007\". Field-level provenance (A/B/C/D) is documented in analysis/OlhoffCurrent/+impl/architecture/docs/SCIENTIFIC_CONFIG_PROVENANCE.md and is printed per run by olh.config.describe."
    }
  },
  "cross_method_warning": "Count and time columns represent method-native computational stages and are not mathematically identical across methods. Total wall time is the common performance quantity."
}
