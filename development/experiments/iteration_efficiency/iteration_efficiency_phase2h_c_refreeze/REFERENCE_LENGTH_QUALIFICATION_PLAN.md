# Reference-length qualification plan

Status: **required; not executed**.

On complete Candidate C reference trajectories, establish the frozen `B_ref=3200`,
`P=100`, `L_ref=500`, and `epsilon_ref=0.001` result, then perform preregistered longer
reference-only continuation sufficient to test whether the floor and freeze decision are
stable. Report every evaluator, method, mesh, gain, floor, and status; do not change the
frozen constants from observed ranks.

The pass artifact must use schema `candidate_c_reference_length_qualification_v1` and
bind the classifier, evaluator and contract hashes, route set, trajectory fingerprints,
tested continuation length, exact acceptance rule, results, and `pass=true`.

