# Audit preregistration

Frozen before geometry, gradient, KKT and derivative calculations. Phase-0 file discovery and hash inspection have already occurred: all five primary MAT containers match their authoritative manifests; the 400 S3 column is iteration 466, not final column 505. No derivative results have been inspected.

ZERO optimization runs, ZERO optimizer calls, ZERO density updates. Only read retained data, call FE/eigen/sensitivity kernels at frozen states, and perform explicitly labelled finite differences. Do not call olhoffSolve, olhoffOpt, innerLoop, mmasub, or repository tests that might solve. Production and all prior evidence remain unchanged. All output stays here; no runs directory.

Primary states: 400x50 S3 @466 (exact counterfactual from three_rung_architecture); 480x60 @386 and 800x100 @468 (three_rung_canary_preflight). Frozen controller [0.04,0.02,0.01], E=A OR B, stageExhaustion, no beta authority. A counterfactual endpoint is explicitly distinguished from a standalone run.

Phase-0 gate: check manifested container SHA-256, recorded density hashes, dimensions, config, source file hashes, events and correct endpoint columns. Failure stops scientific analysis. Record full resolved saved configs, without overwriting them.

Geometry: rho is the saved physical/design field on [0,8]x[0,1]. Gray 0.1<rho<0.9, mid 0.4<=rho<=0.6. Four-neighbour gray connectivity; also report eight-neighbour sensitivity. Euclidean distance to non-gray element centres uses physical spacing. Domain boundary is not counted as a material interface. Broad core = gray elements at distance >R=0.06 from non-gray elements; component maximum depth and diameter proxy 2*depth are not exact member thickness. Separately measure distance to the rho=0.5 threshold interface. Histograms 50 equal bins; quantiles 0,1,5,10,25,50,75,90,95,99,100 percent.

Reconstruct all N=2 generalized stiffness/mass tensors and next-mode derivatives with native kernels. Diagonal terms use their own lambda; off-diagonal mass term uses lambda1. Apply the exact sensitivity filter to all blocks. Evaluate deltaLambda at zero increment and at deterministic analysis directions, without solving any subproblem. A simple-mode derivative is used only when the frozen spectrum and offset subproblem have a resolved simple lowest eigenvalue.

Stationarity: minimize -lambda1/lambda1_ref with volume c=(sum rho-0.5*NE)/(0.5*NE)<=0 and rho in [0.001,1]. The reference lambda is held constant for differentiation. For a resolved simple minimum, spectral weight is 1 on the lowest branch; inactive mode constraints have zero multiplier. At genuine degeneracy reconstruct a PSD trace-one cluster dual or classify inconclusive. Never treat fixed N=2 as proof of exact degeneracy. Reconstruct nonnegative volume dual by least squares on free elements (bound tolerance 1e-7), with a gray-only fit as a robustness check. Report reduced gradients and projected bound-sign violations, normalized by RMS objective gradient over all interior elements. Strong nonstationarity diagnostic: gray RMS >0.1 of this scale, with distribution and fit sensitivity disclosed; this threshold is an audit convention, not a solver theorem.

Critical distinction: the sensitivity filter changes derivative vectors, not rho. It is not a density-map chain rule. Compute physical-problem KKT using exact raw d/d rho and algorithmic frozen-subproblem residual using filtered blocks separately. Do not claim that the latter is KKT of an undisclosed scalar objective. If this distinction prevents a valid central classification, issue INCONCLUSIVE and stop without a scientific experiment recommendation.

FD sample, selected before gradients: each density class solid (>0.9), void (<0.1), gray, mid, broad-core; sort eligible elements by column-major element ID and take nearest-rank 20%,50%,80%, deduplicate. Empty classes explicitly skipped. Steps 1e-3,3e-4,1e-4; centred where feasible without crossing rho bounds or mass-law cutoff, otherwise second-order one-sided into the feasible box. Retain all results. Raw lambda1 FD; also frozen subspace minimum and filtered subspace directional derivatives. Filtered vectors are not expected to match physical objective FD. Compare errors to RMS class derivative scale as well as per-element relative error. If eigensolver roundoff dominates weak derivatives, report inconclusive for those samples rather than reject solely on relative error. Material unexplained disagreement triggers stop. No sample replacement based on result.

Trajectories: compute grayness every saved iteration; note hist.omega is evaluated before that iteration's density update, while RHO is post-update. Align hist(k+1) to RHO(k) where available; reevaluate endpoint FE. Warning association is temporal/descriptive, no unsupported causal counterfactual. Broad-core time series, stage summaries, terminal windows, warning intervals.

No projection/p-continuation/campaign is executed. Rank causes only from measured evidence and respect stop conditions. The 160 filter-support anomaly is excluded.
