# POST-HOC OFFLINE ARCHITECTURE AUDIT

This is a postmortem of already generated trajectories, not withheld validation
or a prospective causal experiment. The full task contract is retained verbatim
in `evidence/REQUEST.txt`; every phase, table, figure, question, scope lock and
decision rule in that contract applies. This plan is frozen before substantive
replay. Prior numerical results are known hypotheses, not independently verified
results of this audit.

## Evidence gate and scope

Audit starting branch/HEAD and dirty state are in `evidence/start.json`. Preserve
preexisting edits. Hash the canonical source manifest, all source files, both
prior preregistrations and authoritative rule code. Verify every used raw input
against its durable recorded manifest. Record MATLAB installation/build and
distinguish original and regenerated artifacts. Preserve original CSV/records
when comparing regenerated C320 evidence. An artifact being present is not
enough: require a readable complete trajectory, expected dimensions, full
telemetry agreement and source/configuration provenance before accepting it.

At task start C320 regeneration PID 63654 is active, its raw trajectory absent,
and prior P15 fails. Inventory and plan preparation may proceed while waiting;
substantive trajectory replay and final scientific conclusions may not. Check
completion without starting another optimization. If regeneration fails, only
recovery/export from already retained numerical data is allowed here. Do not
restart `cv_run` or any optimizer. If indispensable evidence cannot be recovered
without a new optimization, issue `OFFLINE_ARCHITECTURE_EVIDENCE_INCOMPLETE`
and stop. Pending external evidence is not a completed audit or a scientific
refutation.

Zero new scientific optimization runs. No production edits, promotion, causal
validation, campaign, physical/configuration changes, parameter search, Branch C,
mesh constants, new dwell values, new thresholds or new ladder. Mechanics tests
must not accidentally invoke a scientific mesh through an existing suite.

## Rule recovery and indexing

Read frozen normative definitions and authoritative implementation directly.
Export a machine-readable rule including amplitude, cancellation, recurrence,
coherence, tolerance law, all inequalities, lag/window support, NaN handling,
persistence, resets, declaration, transition and terminal ordering. Independently
derive first computability and persistent firing after arbitrary stage start s.
Distinguish first raw predicate, beginning of a persistent block, declaration and
next-stage application. Compare normative sections with the stated arithmetic
predictions in section 8; select exactly one of the requested discrepancy
classifications without modifying the implementation. Test indexing, partial
NaN windows, zero norms, resets and persistence with constructed arrays.

## Trajectory accounting

Primary candidates: C160, C320, C400. Primary mechanism arms: retained F160,
F240 runD, extended F320 runB, extended F400 runC. Inventory shorter original
fixed-move arms and all other relevant retained trajectories; include valid
additional arms in replay and false-positive accounting with distinct IDs.
Production trajectories are observational false-positive/prefix checks only.
Do not silently replace an extended history by a shorter namesake.

Partition stages by the move actually executed at every outer iteration.
Stage costs include every executed iteration from start through end. Incremental
density metrics use the state immediately preceding the stage through its last
state, so contributions telescope. Report initial frequency unavailability
explicitly if no frequency at rho0 was retained; never replace it by a fabricated
initial eigenanalysis. Check whether frequency telemetry belongs to the pre-update
or post-update density; retain and explain its actual alignment. Report both
endpoint frequency and trajectory frequency with explicit indices if needed.

Calculate all requested endpoint/delta/relative metrics, density L2, RMS and
L-infinity displacement, work fractions, beta/native-stop behavior and marginal
efficiencies. Use raw inner iteration counts. Use per-iteration wall times only
if retained; otherwise mark stage wall times unavailable. A work-weighted time
allocation is not a measurement and must not be presented as one. Never combine
regenerated wall totals with original stage times without explicit provenance.

Classify all twelve candidate stages using event timing and dynamics. An exact
floor hit with no independent new transition is FLOOR_EVENT; a later event needs
independent dynamical evidence for DYNAMICAL_EVENT. Near-floor events with that
evidence are MIXED_EVENT. An undeclared/capped stage is NO_EVENT. Do not infer
maturity from endpoint grayness or declare unobserved stage events.

## Single repair and exhaustive false-positive audit

Test only A_LOWAMP: original A with only its amplitude-exclusion clause removed.
Keep B, cancellation statistics, persistence, windows, indexing and resets exact.
Replay original and repaired branches on recorded stage boundaries. Do not
simulate an altered move schedule using the old trajectory after a changed
descent: that portion is a conditional diagnostic, not a live causal outcome.

Record first raw predicate, first persistent event and all persistent firing
iterations/contiguous intervals, for each branch and trajectory/stage. At every
repaired firing, record signed endpoint improvement, best subsequent improvement
and density displacement, both within the recorded stage and to recorded final.
Distinguish material future evolution from numerical oscillation using the full
time series and existing scientific criteria, without tuning a new scalar
threshold to outcomes. Publish the complete event ledger, including adverse
examples and events shared with original A. Distinguish newly introduced false
positives from inherited original-rule limitations. Do not rescue failures with
another condition. Censored histories cannot establish absence of future useful
evolution; scientific gains at lower move values do not alone refute exhaustion
at the current fixed move.

## Counterfactuals and history cost

Label stage-end comparisons RECORDED-TRAJECTORY TRUNCATION COUNTERFACTUAL.
Compute S1/S2/S3 endpoints and final, all requested scientific metrics and
distance to final. Independently locate C320 terminal-cycle onset, cancellation
support, amplitude exclusion, coherence failure and first repaired persistent
event. Separate onset, first eligibility and first persistent declaration.
Recompute outer/inner/wall tail costs and scientific evolution from raw arrays;
test the 91.5% and x2.98 claims with explicit numerator, denominator and interval.
Use OFFLINE COUNTERFACTUAL ESTIMATES for ratios at the repaired recorded state.

Separate time before first predicate evaluability, persistence accumulation and
inclusive time through earliest declaration. Quantify outer/inner/measured-wall
history-floor costs with clearly stated inclusive boundaries and denominators.
Test the 53% C160 claim; do not describe all mandatory observation time as
scientifically useless merely because it is required by the rule.

## Freeze measurements before architecture assessment

Complete and hash metrics, all-event ledger and measurement tables before
assessing R/F/L/X. Follow exactly the architecture decision rules in the retained
request. Assess stage benefits separately (M_nd, omega1, density evolution) and
costs separately; no arbitrary combined quality score. Distinguish removing the
terminal C320 waste from judging the value of S4 itself. Do not prefer a cheaper
architecture without its required scientific support. If evidence cannot choose
one family, retain the inconclusive route. Even if F/L wins, select no policy
parameters, dwell counts, thresholds or move values.

## Outputs and completion

Produce all requested documents, Tables A-D, twelve figure families, reproducible
offline scripts, METRICS.json, DATA_MANIFEST.json and FINAL_SHA256.txt only as
supported by the completed evidence gate. Answer all 46 report questions and
issue exactly one verdict in each requested family after completed analysis.
Until then status files explicitly say pending and contain no final scientific
verdict. Always retain PRODUCTION_CONTROLLER_NOT_PROMOTED and
NINE_MESH_PERFORMANCE_CAMPAIGN_BLOCKED. Never authorize any new run in this task.
